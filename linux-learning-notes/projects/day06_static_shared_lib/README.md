# Day 6 - Static and Shared Libraries

This project demonstrates packaging a small log module as:

- `liblog.a`: static library
- `liblog.so`: shared library

The main teaching steps are provided in the conversation. This README is only a short project index.
